#!/bin/rc
flagfmt='d,l,t,v,b stem'
argv0=yacc
args='grammar'

bifs=$ifs
ifs=''
if(! eval `{aux/getflags $*}) {
	aux/usage
	exit usage
}
ifs=$bifs

y=-S
if (~ $#flagd 1) y=($y -d)
if (~ $#flagt 1) y=($y -D2)
if (~ $#flagv 1) y=($y -v)
if (~ $#flagb 1) y=($y -s $flagb)
exec /$cputype/bin/yacc $y $*
